REPEAT

*   cuando se inicialice todo formulario de agregar document.getElementById("myform").reset();

*   buscar "book-shopping-reten-add";
    var patron2 = "book-shopping-add";
    var patron3 = "book-shopping-edit";
    y reemplzar por la constante en el modelo de fact compra por las constantes
    const factcompraadd = <?php echo \App\Models\FactCompra::FCOMPRAADD; ?>


*   pipes en reaidad es un filtro
    https://pineco.de/laravel-blade-filters/
    
    *   convertir 
        IN_EX
        IN_EXO
        IN_NS
        en constantes y dnde aparescan esto estaba en 
            form-prod
            form tipoCompra 

do


      //validation for creating
    *   validar al crear facturas si tiene existecias


doing

    *   hacer pagina de falla de conexion a internet


does


    *   para todas las relciones cambiar fk_usuarios por fk_company ya que los datos pertenecen a al empresa y no al usuario
        https://www.youtube.com/watch?v=EFs8VIgH2o8&ab_channel=MaestroPedroTV

            $empre = auth()->user()->empre()->active();
        "fk_company" => $empre->id,


                $table->unsignedBigInteger('fk_company')->nullable();
                $table->foreign('fk_company')
                    ->references('id')->on('empres')->nullable();


    * donde salga numCampoActual no funciona por que se esta usando mal el llamado con el eemento lso inputs son con []
        * esta mal document.form1.elements['fk_inventario' + i]

        *   donde aparezca reemplazar por 
            document.form1.elements['numCampos']
            var n = 0
            if (document.form1.fk_inventario)
            n = document.form1.fk_inventario.length ? parseInt(document.form1.fk_inventario.length) : 1;
        
            inicializar numCampoActual en cero o lo que seria formProdCurrent

    *   exterminar codFactProvee
    *   /inventario-list error timeout
    * ../../includes_SISTEM/include eliminar donde parezca
    *   corregir la fecha quetarle la hora antes de cargar editar con el modal de agregar prod

    * corregir error en editar para cuando se cagan los select 

    * mmProd terminar este modal para efitar productos

    * revisar que pasa con contraseña erronea el login

    * cuando ponaga en la tabla de empresa una nueva ponga le rif J2558888 

    * agregar proveedor despues de dare editar el formulario queda lleno
    *   terminar editar y eliminaar cuetas

    crear reporte segun todos los reportes de http://127.0.0.1:8000/inventario-list
    <a target="_blank" href="reporte_inventario.php?id= $inv_cod"> detalles </a>
    se pasa id param

    se daño movimiento de unidades
    crear servicios en laravel
    intervenier y crear una solcion para validar_repetidoM crear un inerceptor y validar la existencia se cambio por validarRepetido
    error en listar ventas los montos no cuadran
    SIMILAR A LA ANTERIOR EXTERMINAR showHint1
    falta agregar editar eliminar cliente proveedores

    CRACION DE FACTURA COMPRA
        al crear validar lo que hace esta funcion en serie_fact_compra
        al crear validar lo que hace esta funcion en num_fact_compra
        onblur="codFactProvee('form1', 'serie_fact_compra', 'num_fact_compra', 'fk_proveedor', 'id_fact_compra')"

    exterminar esta aparicion con event numCampoActual por formProdCurrent
    formProdCurrent = $('div.formProdContent').index(inputOrigin.parents('.formProdContent'))

    para urlActual
    //origen
    let urlCurrent = window.location.href
    let origin = urlCurrent.split('/')
    const urlActual = origin[origin.length - 1]

    en php es por origin con las var de FACTCOMPRA MODAL FCOMPRAADD
    EX javascript :
    const factcompraadd = '<?php echo \App\Models\FactCompra::FCOMPRAADD; ?>'
    inventario-report
    ya imprimie comprobantes de retencion
    reviar por que en el campo al editar N° Planilla de Importación: tiene una hora y porque all crear esta en la base de datos
    login
    hacer la migracion con faltan lo insert para laravel
    https://laravel.com/docs/8.x/pagination#using-bootstrap
    https://appdividend.com/2022/03/01/laravel-bootstrap-modal-form-validation/
    https://getbootstrap.com/docs/5.3/components/modal/#how-it-works
    http://localhost:8000/book-shopping-add
    voy en el modal de los productos



-----
crednciales
------
bd cloud
------
cuenta ings.ivandiaz
yimsystem-bd
v24150144   